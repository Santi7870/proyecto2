@{
    ViewData["Title"] = "Hoodie de Algodón";
}

<h2 class="text-center">@ViewData["Title"]</h2>

<div class="product-details-container">
    <div class="row">
        <!-- Imagen del Producto -->
        <div class="col-md-6">
            <img src="https://images.asos-media.com/products/sudadera-marron-extragrande-con-capucha-y-estampado-de-calavera-con-alas-en-la-espalda-de-vans/205118508-2?$n_640w$&wid=513&fit=constrain" alt="Hoodie de Algodón" class="img-fluid" width="500" height="500">
        </div>

        <!-- Detalles del Producto -->
        <div class="col-md-6">
            <h3>Hoodie de Algodón</h3>
            <p>Este hoodie de algodón es la opción perfecta para el día a día. Confeccionado con algodón 100%, es suave al tacto, ligero y muy cómodo para el uso diario. Ideal para un estilo relajado y casual.</p>
            <h4>Precio: $45.00</h4>
            <p><strong>Envío:</strong> Gratis</p>

            <!-- Opciones de Color -->
            <h5>Colores disponibles:</h5>
            <select class="form-select" id="color-select">
                <option value="blanco">Blanco</option>
                <option value="negro">Negro</option>
                <option value="azul">Azul</option>
            </select>

            <!-- Opciones de Talla -->
            <h5>Tallas disponibles:</h5>
            <select class="form-select" id="size-select">
                <option value="S">S</option>
                <option value="M">M</option>
                <option value="L">L</option>
                <option value="XL">XL</option>
            </select>

            <!-- Botones de acción -->
            <div class="mt-3">
                <button class="btn btn-success" onclick="crearAlertaComprar()">Comprar</button>
                <button class="btn btn-warning" onclick="agregarAlCarrito()">
                    Agregar al carrito
                </button>
            </div>
            <div>
                <a href="@Url.Action("holamundo", "Account")" class="btn btn-secondary mt-4">Regresar</a>
            </div>
        </div>
    </div>
</div>

<script>
    function crearAlertaComprar() {
        var respuesta = confirm("¿Estás seguro de que deseas comprar este producto?");

        if (respuesta) {
            comprarDirecto();
        }
    }

    function comprarDirecto() {

        const color = document.getElementById("color-select").value;
        const talla = document.getElementById("size-select").value;
        const nombre = "Hoodie de Algodón";
        const precio = 45.00;


        window.location.href = `@Url.Action("ComprarDirecto", "CarritoItems")?nombre=${nombre}&color=${color}&talla=${talla}&precio=${precio}`;


        setTimeout(function () {
            alert("¡Compra realizada con éxito!");
        }, 1000);
    }


    function agregarAlCarrito() {
        const color = document.getElementById("color-select").value;
        const talla = document.getElementById("size-select").value;
        const nombre = "Hoodie de Algodón";
        const precio = 45.00;

        window.location.href = "@Url.Action("AgregarAlCarrito", "CarritoItems")?nombre=" + nombre + "&color=" + color + "&talla=" + talla + "&precio=" + precio;
    }
</script>


