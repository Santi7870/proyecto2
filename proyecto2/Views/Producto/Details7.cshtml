@{
    ViewData["Title"] = "Zapato Formal";
}

<h2 class="text-center">@ViewData["Title"]</h2>

<div class="product-details-container">
    <div class="row">
        <!-- Imagen del Producto -->
        <div class="col-md-6">
            <img src="https://media.istockphoto.com/id/639425816/es/foto/dos-pies-de-hombre-en-zapatos-de-moda-y-pantalones-negros.jpg?s=612x612&w=is&k=20&c=w3DZSikN6L_j_ZVNEMU4rkkhoEopJ0u9jJ9v_j9tzRs=" alt="Zapato Formal" class="img-fluid" width="500" height="500">
        </div>

        <!-- Detalles del Producto -->
        <div class="col-md-6">
            <h3>Zapato Formal</h3>
            <p>Estos zapatos formales están diseñados para ocasiones especiales, reuniones de negocios y eventos importantes. Confeccionados con cuero de alta calidad y una suela resistente, estos zapatos ofrecen tanto elegancia como comodidad durante todo el día.</p>
            <h4>Precio: $100.00</h4>
            <p><strong>Envío:</strong> Gratis</p>

            <!-- Opciones de Color -->
            <h5>Colores disponibles:</h5>
            <select class="form-select" id="color-select">
                <option value="negro">Negro</option>
                <option value="marron">Marrón</option>
                <option value="gris">Gris</option>
            </select>

            <!-- Opciones de Talla -->
            <h5>Tallas disponibles:</h5>
            <select class="form-select" id="size-select">
                <option value="40">40</option>
                <option value="41">41</option>
                <option value="42">42</option>
                <option value="43">43</option>
                <option value="44">44</option>
            </select>

            <!-- Botones de acción -->
            <div class="mt-3">
                <button class="btn btn-success" onclick="crearAlertaComprar()">Comprar</button>
                <button class="btn btn-warning" onclick="agregarAlCarrito()">
                    Agregar al carrito
                </button>
            </div>
            <div>
                <a href="@Url.Action("holamundo", "Account")" class="btn btn-secondary mt-4">Regresar</a>
            </div>
        </div>
    </div>
</div>

<script>
    function crearAlertaComprar() {
        var respuesta = confirm("¿Estás seguro de que deseas comprar este producto?");

        if (respuesta) {
            comprarDirecto();
        }
    }

    function comprarDirecto() {
        const color = document.getElementById("color-select").value;
        const talla = document.getElementById("size-select").value;
        const nombre = "Zapato Formal";
        const precio = 100.00;

        window.location.href = `@Url.Action("ComprarDirecto", "CarritoItems")?nombre=${nombre}&color=${color}&talla=${talla}&precio=${precio}`;

        setTimeout(function () {
            alert("¡Compra realizada con éxito!");
        }, 1000); 
    }

    function agregarAlCarrito() {

        const color = document.getElementById("color-select").value;
        const talla = document.getElementById("size-select").value;
        const nombre = "Zapato Formal";
        const precio = 100.00;

        window.location.href = "@Url.Action("AgregarAlCarrito", "CarritoItems")?nombre=" + nombre + "&color=" + color + "&talla=" + talla + "&precio=" + precio;
    }
</script>



