@{
    ViewData["Title"] = "Bienvenido";
}

<div class="welcome-container text-center">
    <h1 class="display-4">¡Bienvenido!</h1>
    <p>Por favor, elige una opción para continuar:</p>
    <div class="button-container">
        <a href="@Url.Action("Login", "Account")" class="btn btn-primary btn-lg">Iniciar sesión</a>
        <a href="@Url.Action("Register", "Account")" class="btn btn-secondary btn-lg">Registrarse</a>
    </div>
</div>

